# Project 4: A Simple Image Library

> Part of [SUSTech CS219 Projects](../README.md)

## 📖 Introduction

本项目构建了一个现代化的 **C++17 泛型图像库**。

目标是模仿 **OpenCV (`cv::Mat`)** 的核心设计理念，利用 **Template** 实现对不同数据类型（`unsigned char`, `float`）和通道数的支持，并通过 **`std::shared_ptr`** 实现了自动内存管理与**引用计数**，彻底解决了 C++ 中的内存泄漏痛点。

## 🖋 Feedback

Grading: 95/100

Comment from Instructor: image_proc.cpp中出现两次#include   

my bad :( 
ddl越长，ddl越短。这个project做的太仓促了，望谅解。
